# Endpoint opcional para o formulário de contato.
# Se definido, o formulário envia um POST (JSON) para esta URL.
# Compatível com serviços como Formspree ou Web3Forms.
# Se ficar vazio, o formulário abre o cliente de e-mail do visitante (mailto).
VITE_CONTACT_ENDPOINT=
